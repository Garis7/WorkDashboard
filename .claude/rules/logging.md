# ロギング

## 原則
- モジュールごとにロガーを定義する
- 例外ログでは `exc_info=True` を使う
- 外部I/O、外部API、DB、ジョブ実行など重要境界でログを出す
- 個人情報、秘密情報、トークン、パスワードを出力しない
- リトライ、フォールバック、失敗理由を追えるようにする

## 追加推奨
- 長時間処理や重要状態遷移で開始/終了ログを出す
- `request_id` / `job_id` / 相関ID、duration を必要に応じて含める
